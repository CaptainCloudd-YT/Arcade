CODE TEMPLATE:
//This is a single line comment (notes that won't be read as instructions to the computer).
//Different syntax is used for comments depending on the language.

League of Legends
Henry Chen


if (teemo use q) {
    blind = true;
} else {
    blind = false;
}

if (teemo use r) {
    poison = true;
} else {
    poison = false;
}

if ([player choose teemo]) {
    player = banned;
} else {
    player = notbanned;
}

//in summary, don't play the RAT
